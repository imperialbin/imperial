import { editingState, languageState } from "./editor";
import { activeModal } from "./modal";

export default {
  editor: {
    languageState,
    editingState,
  },
  modal: {
    activeModal,
  },
};
