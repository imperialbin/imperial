import { editingState, languageState } from "./editor";
import { atomActiveModal } from "./modal";

export default {
  editor: {
    languageState,
    editingState,
  },
  modal: {
    atomActiveModal,
  },
};
